# eudplib 이식 — 제작자가 할 일

만든 날 2026-09-18. 이 문서는 **사용자(제작자)가 할 일만** 모은 것입니다. 항목별 검증 상태는 Claude 가
`MapSource\EUDPLIB_PORT_CHECKLIST.md`(같은 저장소 최상위)에 기록합니다 — 거기 '결과' 칸은 이제 Claude 가 채웁니다.

결과는 **번호와 함께** 알려 주시면 됩니다(화면 사진이면 파일 이름만). 자동 판정으로 잡히는 것은 알려 주지 않아도 기록됩니다.

맵 위치: `C:\Program Files (x86)\StarCraft\Maps\` 의 `eudext_check\`(eudext 확인 맵), `eud_port\`(맵 이식판), `eud_auto\`(브리지 예제·자동 판정판).

## 1. 먼저 (한 번만)

| # | 할 일 |
|---|---|
| P-1 | **런처 세이브 백업**: `C:\Users\whatd\Downloads\SCR_DB_Launcher\SCR_DB_Launcher\Data` 폴더를 통째로 복사해 둡니다(UE_RE 이식판은 실제 세이브 키를 씁니다) |
| P-2 | SCR_DB 가 필요한 맵(eudext 14·14b, UE_RE 이식판)은 런처를 켠 채 엽니다 |
| P-3 | 2인 확인용 상대 PC 에 같은 맵 파일을 복사합니다. SCR_DB 2인은 `eudext\work\win_maps\` 의 매니페스트도 상대 PC 런처 `manifests\` 에 넣습니다 |
| P-4 | **끝난 것**: G-1(디버그 브리지 블록이 SC:R 에서 찾히는가) — 2026-09-18 통과. 이 덕분에 아래 자동 판정이 가능합니다 |
| P-5 | **저장소가 옮겨졌습니다**: 이식 중인 다섯 맵은 이제 각자 비공개 저장소입니다 — `Documents\theSeed_eud` · `MSF_Respect_V_eud` · `MSF_UE_RE_eud` · `MSF_Memory_2_eud` · `MSF_Memory_eud`. 아래 표의 RUN.bat 경로도 새 자리로 고쳐 두었습니다. 각 저장소에서 `build.bat` 을 더블클릭하면 한 번에 빌드됩니다(BGM 은 저장소 밖 `C:\euddraft0.9.2.0\<맵>_BGM`) |

## 2. 자동 판정 (맵을 열어 두기만)

**전부 준비됐습니다. eudext 맵은 2026-09-18 07:02 에 다시 빌드해 `Maps\eudext_check\` 에 넣어 두었습니다** — 통합 맵에서 **화면 줄(display) 항목을 떼어** 따로 돌리게 바꿨습니다(지난번 EUD ERROR 가 났던 자리).

| 순서 | 무엇 | 먼저 켤 것(더블클릭) | 그다음 열 맵 | 걸리는 시간 |
|---|---|---|---|---|
| **0-1** | **eudext 통합 자동 점검 — 이것부터** | `C:\Users\whatd\Documents\eudext_dbg\work\ingame_auto_maps\RUN.bat` | `Maps\eudext_check\00_auto_all.scx` — **혼자**, 열어 두고 기다리기. **이번엔 끝까지 가야 정상입니다** | 4분 30초 |
| **0-1b** | 화면 줄 오류 자리 좁히기 (0-1 뒤) | 같은 RUN.bat (켠 채) | `Maps\eudext_check\25b_display_bisect.scx` — **혼자**. **멈출 수 있는 맵입니다**(버려도 되는 방에서). 멈추면 그대로 두고 `RUN.bat --once` 를 한 번 | 20초 |
| **0-1c** | 화면 줄 본 점검 | 같은 RUN.bat (켠 채) | `Maps\eudext_check\25_display_check.scx` — 멈추면 역시 `RUN.bat --once` | 1분 |
| 0-2 | eudext 나머지 맵 (조작·소리·런처·위험) | 같은 RUN.bat (켠 채) | `Maps\eudext_check\` 의 나머지 맵 — 순서와 조작은 같은 폴더 `README.md` | 맵마다 1~5분 |
| A-1 | theSeed S0 (제한판) | `Documents\theSeed_eud\eud\auto\RUN.bat` | `theSeed_S0_dbg_t1.scx` — **승인 닉네임**(GALAXY_BURST / Natori_sana)으로 혼자 | 10분 이상 두면 좋음 |
| A-2 | theSeed S0 (테스트판·공개판) | 같은 RUN.bat (켠 채) | `theSeed_S0_dbg_t2.scx` → `theSeed_S0_dbg_t0_noprobe.scx` | 각 2~3분 |
| A-3 | Respect V 1단계 (정식판) | `Documents\MSF_Respect_V_eud\eud\auto\RUN.bat` | `RespectV_S1_dbg.scx` — **멀티 방(LAN)** 으로(싱글은 튕깁니다). **2026-09-18 05:21 에 새로 빌드한 판입니다**(방장 선택 유닛 재생성이 들어갔습니다 — 난이도 선택 뒤 선택 유닛이 생기고 **속도 비콘 43~46** 도 됩니다) | 5분 |
| A-4 | Respect V (시험판·시험 시작판) | 같은 RUN.bat (켠 채) | `RespectV_S1_dbg_T1.scx` → `RespectV_S1_dbg_T2.scx` | 각 3분 |
| B-1 | UE_RE S0 (배포판과 같게 도는가) | `Documents\MSF_UE_RE_eud\eud\auto\RUN.bat` | `UE_RE_S0_dbg.scx` — **런처를 켠 채**(P-1 백업 먼저). 로비 불러오기·키·칭호 채팅·HOME 저장을 해 보고 1분 두기 → 끝낼 때 채팅 닫고 **F8 1초** | 10분 |
| B-2 | UE_RE 보호판 | 같은 RUN.bat (켠 채) | `UE_RE_S0_cplp_dbg_out.scx` (B-1 이 되면) | 5분 |
| B-3 | Memory 2 S0 | `Documents\MSF_Memory_2_eud\eud\auto\RUN.bat` | `Mem2_S0_dbg.scx` — 혼자. 난이도·모드 고르고 조금 진행 → **F8 1초**. 보호판은 `Mem2_S0_cplp_dbg_out.scx` | 10분 |
| B-4 | Memory 1 S0 (출시판 Library) | `Documents\MSF_Memory_eud\eud\auto\RUN.bat` | `Mem1_S0_A_dbg.scx` — **멀티 방**에서 1인(싱글은 원본이 막습니다) → **F8 1초** | 10분 |
| B-5 | Memory 1 (지금 Library·배포 모양) | 같은 RUN.bat (켠 채) | `Mem1_S0_B_dbg.scx` → `Mem1_S0_C_dbg_out.scx` | 각 5분 |

**0-1 에서 봐 주실 것 두 가지** — ① 맵이 **끝까지 가는지**(지난번 EUD ERROR 가 났던 항목은 떼어 냈습니다), ② 화면에 `[D14-0] … 첫 탄 epd` 줄이 지나갈 때 그 값이 **0 이 아닌지**(탄막이 0발이던 것을 고친 자리입니다. 0 이면 고친 방향이 틀린 것이니 그 줄만 알려 주세요).

**0-1b 는 일부러 멈춰 볼 수도 있는 맵입니다.** 18단계를 하나씩 넘기며 어느 쓰기에서 EUD ERROR 가 나는지 가립니다 — **몇 단계에서 멈췄는지**만 알려 주시면 원인이 잡힙니다. 끝까지 가면 그것대로 답이 됩니다.

- 판정 결과 파일(제가 읽습니다): theSeed·Respect V 는 각 `eud\auto\results\results.json`, UE_RE·Memory 1·2 는 각 `eud\out\auto\results.json`.
- 하이브리드 세 맵(B-1~B-5)은 **끝낼 때 채팅창을 닫고 F8 을 1초 누르면** "끝" 표식이 남습니다(세 맵 다 안 쓰는 키입니다).
- 맵이 멈추거나 튕긴 뒤에는 같은 RUN.bat 을 `once` 로 한 번 더 돌리면(`RUN.bat once`) 어디서 멈췄는지 찍힙니다.
- theSeed 제한판에서 **Y 키**를 몇 번 눌러 주시면 3절의 조작 항목도 함께 판정됩니다.

**A-1~A-2 는 끝났습니다(2026-09-18 두 번 실행). 자동·조작 항목 전부 통과 — 다시 하실 필요 없습니다.**
남은 theSeed 항목은 사람이 볼 것뿐입니다: 미니맵 밝기(T0-6), 효과음이 5번만 나는지(T0-11), 채널 건물이 안 보이는지(T0-12), 일부러 멈추는 3개(T0-2·4·5 — 6절), 2인 6개(T0-14~19 — 5절).

(참고 — 아래는 지난 안내입니다) 조작이 필요했던 항목 목록:
theSeed S0 는 **뼈대만 옮긴 단계라 오프닝·설정 메뉴·마린·건작이 없습니다**(빈 본진에서 시작하는 것이 정상). 화면에는 Y 를 눌렀을 때만 확인 줄이 뜹니다.
다시 열어 아래만 해 주시면 T0-3·8·9·10·11 이 판정됩니다(t1 판 2~3분 + t0_noprobe 판 30초, RUN.bat 은 켠 채로).

| 맵 | 조작 |
|---|---|
| `theSeed_S0_dbg_t1.scx` | ① 채팅창 닫고 **Y 2번 이상** ② 채팅창을 2초 넘게 **열고 Y** 를 친 뒤 닫기 ③ **Y 로 값 줄을 켜고** 마우스를 맵 **왼쪽 끝·오른쪽 끝**에 둔 뒤 다시 Y ④ **5초 안에 Y 8번** 빠르게 → 5초 쉬고 다시 Y (소리가 5번만 나는지 귀로) |
| `theSeed_S0_dbg_t0_noprobe.scx` | **Y 한 번** (공개판에서는 줄이 안 떠야 정상) |

하실 일의 요령은 이렇습니다.

1. `RUN.bat` 을 더블클릭해 판정 도구를 켭니다(게임 메모리를 읽기만 합니다. 배틀넷에서는 쓰지 않습니다).
2. 도구가 말하는 순서대로 맵을 엽니다. 통합 맵 하나로 eudext 자동 점검 약 91항목이 한 번에 끝납니다(약 3분 30초).
3. 화면에 `단계 … 통과` 가 흐르고, 끝나면 `>>> 다음 맵을 여세요` 가 뜹니다. 결과는 파일로 남습니다.
4. 도중에 멈추거나 튕기면 도구가 **어느 단계에서 멈췄는지** 기록합니다 — 그 줄만 알려 주시면 됩니다.

자동 판정 대상: **87항목**(AUTO). 아래 3절의 조작 항목까지 하면 **116항목**이 자동으로 판정됩니다.

## 3. 조작만 해 주시면 판정은 자동 (29)

판정 도구를 켠 채, 아래 조작을 하시면 됩니다. 결과 판정은 브리지가 합니다.

**eudext 확인 맵 — 필수** (2)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| D14-6 **필수** | 10 bullet_check | ①~⑤ 각각: 클릭 선택됨/드래그 선택됨(선택 표에 epd 포인터가 있나), 서로 밀림(위치 변화 px), 칸 값(만든 직후·ue_re 뒤). 안개 속 보임은 눈 — 사람 몫: 10초 안에 다섯 개를 하나씩 클릭하고 한 번 드래그 |
| D17-6 **필수** | 13 chat_check | ① 그 줄이 이름 없이 '안녕 éè 가나다' 로 한 셀씩 다시 드러남(스냅숏) ② 원문 앞 12바이트 3개·표식 위치 N 기록(chatmodel 수정용) ③ 보통 채팅 줄은 그대로 — 사람 몫: 18초 뒤 채팅으로 '!H안녕 éè 가나다' 와 보통 채팅 한 줄 |

**eudext 확인 맵 — 권장** (8)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| B13-3 | 21 sync_check | 누르는 동안만 held 증가, 떼면 멈춤. 톡 치기(한 턴 안)에서는 held 불변, 점수 +1 이 빠지는 빈도 기록. SPACE 누른 채 채팅창 열고 떼기 → held 멈춤 — 혼자(base_multi 1인)도 가능 — 사람 몫은 키 조작뿐 |
| B13-4 | 21 sync_check | 동기 값이 로컬 좌표를 몇 프레임 늦게 따라오는지(지연 프레임 수 기록), 마우스를 멈춘 동안 값이 흔들리거나 0 으로 튀지 않음 — 배틀넷 비교(B13-10)는 브리지 없이 |
| B13-5 | 21 sync_check | 채팅창 열린 동안 SPACE·LCTRL+좌클릭 → 점수 불변, TAB → panel 불변, F5 는 출력됨 |
| B13-6 | 21 sync_check | F5 를 오래 눌러도 출력 1회(누름 1회당), 우클릭 1회당 1줄. 두 PC 는 각자 화면에만 |
| B13-8 | 21 sync_check | 유닛 여러 기 선택 → SPACE·좌클릭 뒤에도 선택 표가 같고 QC 유닛(49) 포인터가 섞이지 않음. 부대 지정 뒤에도 |
| B15-6 | 04 datpatch_check (변형) | restore=False 로 프레임을 넘긴 temporary 뒤 unpatch_all 로 되돌아가고, 그 사이 저장·불러오기·리플레이 재생에서 값·진행이 정상 — 리플레이 재생 중에도 블록이 돌아 리더로 확인 가능 |
| E6-2 | 25 display_check | 고정 줄 두 개가 같은 slot 에 머물고 숫자만 바뀜 — 사람이 채팅을 몇 번 친 뒤에도(자동: slot 번호 불변) — 사람 몫: 채팅 몇 번 |
| F19-3 | (확인 맵 없음) | 4:3 에서 0, 와이드에서 1, 판정 뒤 화면 위치가 원래대로 — 사람 몫: 해상도 두 가지로 열기 |

**UE_RE 이식판** (2)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| S0-5 | UE_RE_S0.scx (①) | ESC·F9·F12·TAB·Z·Shift+←/→·백틱·1~7·Q/E 누를 때마다 해당 채널 받은 수 +1 (빠뜨림 0, 두 번 0). 상점 구매·콘솔 선택 결과는 눈 — 사람 몫: 키를 한 번씩 |
| S0-7 | UE_RE_S0.scx (①) | @칭호 1~4 채팅 뒤 chatEvent 칸 값 2~5, 숫자 채팅 뒤 기부 금액만큼 광물 이동. 문구·이름 앞 칭호는 눈 — 사람 몫: 채팅 |

**theSeed 이식판** (4)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| T0-8 | theSeed_eud_S0_t1 | Y 누름 엣지마다 yCount[P1] +1, 스위치 199 뒤집힘, [S0] 줄 1개 (누락 0·두 번 0) — 사람 몫: 채팅창 닫고 Y 몇 번 |
| T0-9 | theSeed_eud_S0_t1 | 채팅창이 열린 동안 Y 를 쳐도 yCount 불변, [S0] 줄 없음 |
| T0-10 | theSeed_eud_S0_t1 | 값 줄 켜진 동안 lastX[P1] 이 로컬 X 를 따라가고 recvCount 증가, 꺼진 동안 불변 — 사람 몫: Y 로 켜고 마우스 좌·우 끝, 다시 Y |
| T0-11 | theSeed_eud_S0_t1 | 5초 안 8번 → 소리 5번에서 멈춤(budget 5), 쉬고 나면 0부터. 소리는 귀 |

**Respect V 이식판** (11)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| S1-1-4 | Respect_V_eud_S1.scx (LAN 방) | 선택 유닛을 로케이션 11 로 → EVF 유닛 0, evf=1, 전체 문구. ADEnd 는 귀 |
| S1-1-5 | Respect_V_eud_S1.scx (LAN 방) | 로케이션 48 → OM 유닛 0, om=1, 문구 |
| S1-1-6 | Respect_V_eud_S1.scx (LAN 방) | 비콘 → 난이도 값, gs=1, 선택·난이도·옵션 유닛 0, 광물 증가(1억모드 식 — S1-5-2). bnetclick 은 귀 |
| S1-1-8 | Respect_V_eud_S1.scx (LAN 방) | SC 선택 → P6·P7·P8 색 188·160·173, 컴퓨터 업그레이드 단계 SC 6·3·3 (HD 2·1·1, MX 4·2·2 는 각 판에서). 원본과 나란히는 눈 |
| S1-3-8 | Respect_V_eud_S1_T2.scx | 방장만 62·63 생산 가능(표), 난이도 뒤 BGM 요청 없음(TestStart). 들리지 않는지는 귀 |
| S1-4-2 | Respect_V_eud_S1.scx (LAN 방) | 3·7 한 번씩 → 인식 1, 그 PC 에만 문구, 소리 2번(귀), 다시 눌러도 안 나옴. 칭호 없는 사람은 멀티 |
| S1-6-3 | Respect_V_eud_S1.scx (LAN 방) | 유닛 3 생산 → 끔 비트 1 + 'BGM 을 듣지 않습니다.', 다시 → 0 + '듣습니다.' |
| S1-6-4 | Respect_V_eud_S1.scx (LAN 방) | 끈 동안에도 곡 시간이 흐르고, 켠 뒤 그 곡이 끝나야 다음 요청(X13), 끄기는 다음 프레임부터(X14) |
| S1-6-6 | Respect_V_eud_S1.scx (LAN 방) | F10 일시정지 수 초 뒤 재개: 2.5초 넘는 경과는 0(X15) — 곡 시간이 일시정지만큼 건너뛰지 않음. 겹침은 귀 |
| S1-7-2 | Respect_V_eud_S1.scx (LAN 방) | 비콘 43~46 → SpeedVar 4·6·8·10 + 문구. 체감 속도가 원본과 같은지는 사람(브리지 프레임/초는 보조) |
| S1-7-3 | Respect_V_eud_S1_T2.scx | 방장이 62/63 생산 → +1/−1(1~11 안) + 문구. 방장 아닌 사람 불가는 멀티, 정식판은 모두 불가 |

**Memory 2 이식판** (1)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| M2-S0-3 | Mem2_S0_eudext.scx | 방장 유닛(60·23)으로 난이도·모드·배속 선택(사람), 60초 뒤 모드 유닛 수 0(자동). 표시·배속은 눈 |

**Memory 1 이식판** (1)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| M1-S0-3 | Mem1_S0_A.scx | TAB/ESC 누름 엣지마다 데스 206/205 처리 한 번(두 번·빠뜨림 없음), 첫 사람만. 선택 글·색·소리는 눈 |

## 4. 눈·귀로 보셔야 하는 것 (75)

모양·색·소리·체감은 브리지로 알 수 없습니다. 값은 자동으로 판정되니, **보이는 것만** 봐 주세요.

**eudext 확인 맵 — 필수** (7)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| D14-8 **필수** | 10 bullet_check | 종류=210 (자동). 왼쪽 탄이 가운데(야마토)와 같은 모양인지는 눈 — 총알 표(CBullet weaponType +0x5C 추정, sourceUnit +0x60 추정) 스캔이 확인되면 자동 |
| D16-1 **필수** | 11 bgm_check | ② 660Hz 가 ① 440Hz 와 다른(높은) 음으로 들림 — 귀. 문자열 칸에 d16_b.wav 이름이 들어갔는지는 자동 — 소리는 브리지로 알 수 없음 |
| D16-3 **필수** | 11 bgm_check | ⑤⑥ 틈·딸깍·겹침은 귀. F10 일시정지 뒤 last_dt(>2500 이면 0)·조각 번호 건너뜀은 자동 기록 — 사람 몫: ⑤ 도중 F10 멈춤·풀기 |
| D17-1 **필수** | 13 chat_check | 표식 줄이 찍힌 프레임에 빈 줄 → 다음 프레임부터 셀 하나씩 드러남, 드러난 셀에 '!H'·U+200B 바이트 없음(자동). 네모·물음표가 안 보이는지, 가운데 정렬 움직임이 원본과 같은지(눈) |
| E6-3 **필수** | 25 display_check | 13번째 줄 바이트 = 우리 글(자동), 머리 값 복원(자동). SC 오류 문구가 보이는지·오류 소리가 나는지는 눈·귀 |
| E6-4 **필수** | 25 display_check | 이름 칸이 '[E6-4] 마린 <숫자>' 이고 뒤 잔재 없음(눈 — 마린 선택 필요). TBL 바이트 끝 NUL·숫자 갱신은 자동 |
| E6-7 **필수** | 25 display_check | '\|-10\|1234만5678\|12.5\|42\|42\|' 대괄호 사이에 빈틈 없음(0x0D 가 폭을 차지하지 않음) — 글꼴 렌더링 — 브리지로 판단 불가. D5-8 과 같은 질문 |

**eudext 확인 맵 — 권장** (28)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| B15-7 | 04 datpatch_check (두 변형) | 유닛 선택 음성이 기존 TEP 맵과 같은 쪽(SetUnitsDatX 판 / set 판) — 소리 — 브리지로 판단 불가 |
| B15-10 | 04 datpatch_check | 버튼 활성 동작이 기존 맵과 같음(눈). 칸 값이 매 프레임 0 인지는 자동 |
| D5-2 | 16 numfmt_check | [00042] [-1,234,567] [+0,001,234] 가 기대 줄과 같게 보임 (바이트는 D5-1 이 판정) |
| D5-3 | 16 numfmt_check | 전각 숫자 사이 빈틈 없음, 앞 0x0D×4 가 자리를 차지하지 않음 — 글꼴 렌더링 — 브리지로 판단 불가 |
| D5-4 | 16 numfmt_check | 자리마다 색 P6 갈색 … P1 빨강 |
| D5-5 | 16 numfmt_check | 만 단위 색이 옛 DPS 화면과 같음 |
| D5-6 | 16 numfmt_check | [12.5] [1234.567] [막대 앞 3개 초록] [05] |
| D5-7 | 16 numfmt_check | 한자 글자·16진 표시 |
| D5-8 | 16 numfmt_check | 0x0D 채움 고정 폭이 옛 DisplayPrint 와 같은 간격(옛 맵과 나란히) — E6-7 과 같은 질문(0x0D 폭) — 한 번만 보면 둘 다 답이 됨 |
| D6-1 | 09 strdesign_check | mcf 판 장식이 DPS강화하기·UE_RE 화면과 같음 — 바이트는 에뮬레이터가 원본 Lua 와 대조함 — 남은 건 글꼴·색 |
| D6-2 | 09 strdesign_check | seed 판 장식이 theSeed·Stella_II 와 같음 |
| D6-3 | 09 strdesign_check | respect 판, \x13 줄 가운데 정렬 |
| D6-4 | 09 strdesign_check | memory 판, \r\r 뒤 \x13 가운데 정렬 |
| D6-5 | 09 strdesign_check | \x13 두 번·오른쪽 정렬·줄바꿈 장식 |
| D14-9 | (bullet_example) | height_actions 로 높이·그리기 층이 바뀜, 생성 전에 써야 하는지 |
| D14-13 | 10 bullet_check | BulletDat 만으로 설정한 208 탄 모양이 UE_RE 와 같음(dat 값은 시험이 이미 대조) |
| D16-2 | 11 bgm_check | ③(1번) ④(2번) 크기 차이·빠짐 |
| D16-6 | 11 bgm_check (--ogg 빌드) | ⑨ 확장자만 .wav 인 Ogg 곡이 들림 |
| D16-8 | 11 bgm_check | ⑩ 효과음이 몇 개 들리는지, 스윕 끊김 |
| D16-9 | 11 bgm_check | 방 만들기~게임 시작 시간(초), units_ingame 과의 차이 — 로딩 중에는 블록이 없어 브리지가 시작 시각을 모름 — 초시계 |
| D17-3 | 13 chat_check | é ñ ü ß ç ø Ω 가 깨지지 않게 보임(눈). 드러나는 경계가 UTF-8 2바이트를 자르지 않는지는 자동 |
| D17-7 | 13 chat_check | 2프레임에 한 글자·글자마다 딸깍·공백은 조용(귀). 드러난 비공백 셀 수 = PlayWAV 횟수는 자동 |
| D17-8 | 13 chat_check | 끝난 줄이 시간이 지나 사라짐, 사라지기까지 초, 채팅창을 열면 다시 보이는지 — chatmodel.expire 가정 채우기 |
| E6-4b | 25 display_check | NUL 없이 짧게 쓰면 앞 쪽 글자가 뒤에 남아 보임(눈), 바이트 잔재는 자동 |
| E6-6 | 25 display_check | 임무 목표 창을 연 채로 카운터가 바뀌는지, 다시 열 때만 바뀌는지 — 사람 몫: F10 → 임무 목표 |
| E6-9 | 25 display_check | 긴 줄 끝 '끝(…)' 이 보임(눈). 버퍼에서 그 줄 길이·NUL 위치(218 넘는지)는 자동 |
| E12-7 | 19 spawn_check (화면 줄 [E12-6]) | 이미지 429 원이 번쩍임·모양(눈). 스캐너 유닛 남지 않음(수 0)·이미지 칸 546 복원은 자동 — 화면 줄머리 [E12-6] |
| E12-9 | 19 spawn_check | 끊김 체감 — 브리지는 E12-1·E12-2 구간 프레임 시간 기록 |

**UE_RE 이식판** (7)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| S0-4 | UE_RE_S0.scx (①) | ←/→·B·Y 반응이 배포판과 같음(눈). 키 누름마다 채널 수신 1회인지는 자동 보조 — 시작 레벨 숫자는 CtrigAsm 변수 — ①에서는 못 읽음 |
| S0-6 | UE_RE_S0.scx (①) | 멀티 커맨드로 마린이 마우스 위치로 이동·공격, 연 채 우클릭 이동 |
| S0-8 | UE_RE_S0.scx (①) | 구간별 곡·보스곡, 관전자면 소리 꺼짐 — 소리 |
| S0-9 | UE_RE_S0.scx (①) | 레벨 1→2 클리어 문구·포인트·사망 알림(눈). 건물 147 수 0 → 다음 레벨 전환 시각은 자동 기록 |
| S0-10 | UE_RE_S0.scx (①) | 시작 레벨 2 에서 Identity 패턴·처치·보스곡 |
| S0-11 | UE_RE_S0.scx (①) | 다른 보스 등장·패턴·클리어가 배포판과 같음 |
| S0-15 | UE_RE_S0.scx (①) | 30분(레벨 여러 개) 프레임 하락·멈춤이 배포판보다 심하지 않음 — 배포판에도 같은 표지 시그니처가 있어 같은 리더로 두 판 프레임/초를 잴 수 있음 |

**theSeed 이식판** (1)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| T0-12 | theSeed_eud_S0_t1 | (4,4) 타일 커맨드센터가 안 보이고 드래그로 안 잡힘(눈), 경고 문구 없음·이동 상태 6(자동) |

**Respect V 이식판** (12)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| S1-1-1 | Respect_V_eud_S1.scx (LAN 방) | 첫 프레임 속도 0x1D, 화면 로케이션 64 중심, 150사이클 늘고 149사이클 줄어듦(자동). scan.wav·줄 구성은 원본과 나란히(눈·귀) — 싱글은 실행 방지로 튕김 → 1인 LAN 방(브리지 허용) |
| S1-1-3 | Respect_V_eud_S1.scx (LAN 방) | 클릭한 PC 에만 화면 비움 + CTEnd + 설명(눈·귀). 다른 PC 에 안 보이는지는 멀티 |
| S1-2-1 | Respect_V_eud_S1.scx (LAN 방) | 상태창 수치가 원본과 같음, 레나마린 72·118·무기 127·128 은 HEAD 편집분 |
| S1-2-2 | Respect_V_eud_S1.scx (LAN 방) | 버튼·위치·아이콘이 원본과 같음 |
| S1-2-3 | Respect_V_eud_S1.scx (LAN 방) | 생산 가능/불가·요구조건 문구가 원본과 같음, 62·63 은 정식판에서 아무도 불가 |
| S1-2-4 | Respect_V_eud_S1.scx (LAN 방) | 상태창 특수 표시 14종·와이어프레임 40종 |
| S1-2-5 | Respect_V_eud_S1.scx (LAN 방) | 이름·툴팁·색이 원본과 같음 |
| S1-2-6 | Respect_V_eud_S1.scx (LAN 방) | Z·X·Q·W 업그레이드 툴팁 네 줄(시작값) |
| S1-2-7 | Respect_V_eud_S1.scx (LAN 방) | 난이도 뒤 영웅·보스 체력 dat(2단계에서 다시) |
| S1-6-1 | Respect_V_eud_S1.scx (LAN 방) | NM·HD → 1번 곡(MX 28, SC 29) 요청(자동), 들림(귀) |
| S1-6-7 | Respect_V_eud_S1.scx (LAN 방) | 스캔·ADEnd·CTEnd 등 소리가 원본처럼 들림 |
| S1-8-1 | Respect_V_eud_S1.scx (LAN 방) | (상황이 있으면) '경고: '/'WARNING:' 채팅 줄이 곧 지워짐 — 버퍼에서 자동 확인 — 상황을 만들 수 없으면 생략 |

**Memory 2 이식판** (8)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| M2-S0-4 | Mem2_S0_eudext.scx | 스탯·버튼·요구사항·와이어프레임·한글 툴팁이 0.ZI_Fix 와 같음 |
| M2-S0-5 | Mem2_S0_eudext.scx | 조각 BGM 끊김·겹침 없음, 구간·보스곡, 효과음 |
| M2-S0-6 | Mem2_S0_eudext.scx | 생산·조합·환전·자동환전·기부·업그레이드·컬러·메딕·보호막의 동작·가격·문구가 같음(금액 변화는 자동 보조) |
| M2-S0-7 | Mem2_S0_eudext.scx | 소환 도형·위치·주기가 같음, 한쪽 쏠림·엉뚱한 위치면 즉시 알림(NSQC 로케이션 복구 관련) |
| M2-S0-8 | Mem2_S0_eudext.scx | 킬 점수·EXP·사망 알림이 같음 |
| M2-S0-10 | Mem2_S0_eudext.scx | 평소 빠른 조작에서 BanCode 오탐 없음(값 0 유지 — 자동), 0.ZI_Fix 에서 처벌되던 조작은 처벌(사람 비교) |
| M2-S0-12 | Mem2_S0_eudext.scx | RedNumber·해금·중간보스 등장·패턴이 같음 |
| M2-S0-13 | Mem2_S0_eudext.scx | 프레임이 0.ZI_Fix 와 비슷, 느린 장면 기록 |

**Memory 1 이식판** (12)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| M1-S0-2 | Mem1_S0_A.scx | 시작 위치·건물 배치가 출시판과 같음, 이상한 유닛 없음 |
| M1-S0-5 | Mem1_S0_A.scx | 마린·영웅·적 수치가 출시판과 같음, P1·P2·P4·P5 마린 그림 = P3 마린(16) |
| M1-S0-6 | Mem1_S0_A.scx | 버튼 자리·아이콘·툴팁·단축키가 출시판과 같음 |
| M1-S0-7 | Mem1_S0_A.scx | 업그레이드·스팀팩 활성/비활성이 출시판과 같음 |
| M1-S0-8 | Mem1_S0_A.scx | 비용 숫자가 든 글이 깨지지 않음(줄 길이 그대로) |
| M1-S0-9 | Mem1_S0_A.scx | 건작 모양·주기·안내 글·BGM 전환이 같음, 유닛이 몰려도 튕기지 않음 |
| M1-S0-10 | Mem1_S0_A.scx | 환전·조합·기부·메딕 반응·금액이 출시판과 같음 |
| M1-S0-11 | Mem1_S0_A.scx | BYD 건작이 원본처럼, 비욘드 보스 조각 BGM 끊김 없음(귀) |
| M1-S0-12 | Mem1_S0_A.scx | Core of Depth 연출·조각 BGM(ikasu, H_Boss) |
| M1-S0-13 | Mem1_S0_A.scx | 승리 연출·엔딩·점수·패배가 같음 |
| M1-S0-15 | Mem1_S0_A.scx | 30분 프레임 하락·멈춤이 출시판보다 심하지 않음 |
| M1-S0-16 | Mem1_S0_B.scx | ① 과 같은 조작 결과(특히 센터·SP/CERE/NEX 건작, BYD 도형) |

## 5. 2인·관전자가 필요한 것 (44)

LAN 이면 각 PC 에서 판정 도구를 켤 수 있습니다(배틀넷에서는 켜지 마세요).

**eudext 확인 맵 — 필수** (5)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| B13-1 **필수** | 21 sync_check | 5분 동안 드롭·동기화 오류 없음. 각 PC 리더의 score/held 표가 같은 게임 프레임에서 같음 — 배틀넷에서는 브리지 금지 — LAN 판만 자동 대조 |
| B13-2 **필수** | 21 sync_check | 두 PC 점수가 같다. 로컬 SPACE 누름 엣지 수 대비 점수 증가 수(빠진 횟수)를 기록 |
| D18-6 **필수** | 14b scrdb_check_2p | 두 런처 로그의 플레이어 P1/P2, 한 PC 에만 런처를 켠 판에서도 디싱크 없음, 런처 없는 PC 는 loaded=0 |
| E6-1 **필수** | 25 display_check (관전자 PC) | 관전자 버퍼에 '[E6-1] 모두…'·'관전자에게만…'·'LOCAL: 이 PC 번호 = 128~131' 세 줄, P1 버퍼에는 '관전자에게만' 줄 없음·번호 0 — M1_multi 에 page 0 세 줄만 옮겨 함께 봄 |
| F19-1 **필수** | 23 misc_obs_check | 관전자 2~4번 PC 에서 END/HOME/INSERT 로 모드가 두 번 이상 바뀜(0x68C144 값 변화 자동), 실제 채팅 수신 대상은 사람 |

**eudext 확인 맵 — 권장** (20)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| B8-1 | 20 players_check | 관전자 PC local_player ∈ 128~131(슬롯마다 다름), 플레이어 PC 0~7, contains_obs 는 관전자 PC 에서만 1 — 관전자 PC 에도 리더를 켜면 판정은 자동(LAN 만) |
| B8-2 | 20 players_check | run_as(Observers, DisplayText) 줄이 관전자 버퍼에만 있음. 소리(play_wav)는 귀로 |
| B8-3 | 20 players_check | run_as(list(P1,P2), DisplayText('A')) 줄이 각 PC 버퍼에 정확히 한 번 |
| B8-5 | 20 players_check | 한 명이 나간 뒤 is_human(1)=0, human_mask 에서 비트 빠짐, 나간 플레이어 광물 증가 없음 |
| B8-6 | 20 players_check | 동맹 창·SetAllianceStatus 와 표가 같게 바뀜, run_as(Allies(P1)) 줄은 동맹 PC 에만 |
| B8-7 | 20 players_check | 관전자 CP 로 CenterView·MinimapPing·SetMissionObjectives 해도 오류·튕김 없음, 관전자 화면에만 적용 |
| B8-8 | 20 players_check | 사람 슬롯 0·2·3 처럼 띄운 방에서 모든 PC·관전자에서 contains_user(Everyone)=1 |
| B13-7 | 21 sync_check | 관전자 PC 멈춤·디싱크 없음, 관전자 F5 출력 없음 |
| B13-10 | 21 sync_check | 배틀넷(턴이 여러 프레임)에서 B13-2~4 의 소실·지연이 LAN 과 어떻게 다른지 — 배틀넷 — 브리지 사용 금지, 사람 확인만 |
| B13-11 | 21 sync_check (NSQC 변형) | 값이 오고 못 받은 사이클은 에러코드 — 선택 항목 |
| B13-12 | 21 sync_check (별도 맵) | bus.mouse_location 로케이션이 각 PC 마우스를 따라감, 번호 규칙(첫 로케이션 + p − 가장 작은 사람 번호) — 선택 항목 |
| C11-8 | 22 pool_multi | 모든 PC 에서 t1ok~t7ok 가 같고 디싱크 없음 |
| D14-12 | (멀티 맵) | 매 프레임 bullet 2인 이상 디싱크 없음 |
| D16-4 | 11 bgm_check | 관전자 PC 번호 128~131, 소리 들림(귀), DELETE 로 끄고 켜짐(끔 비트 자동) |
| D16-7 | 11 bgm_check | N 뒤 모든 PC 조각 번호가 같은 게임 프레임에 같음, 방장이 나가도 계속 오름, M 은 누른 PC 만 |
| F19-5 | 23 misc_obs_check | 안내 문구가 보이고 button3.wav 소리 |
| F19-6 | 23 misc_obs_check | 0x68C144 = 2/3/5 일 때 실제 채팅이 가는 곳(특히 3 '대상없음') |
| F19-2 | (확인 맵 없음) | 모든 PC·리플레이에서 HotkeyUnit 값이 같음 |
| F19-1b | (확인 맵 없음) | 모든 PC·싱글·리플레이에서 같은 방장 번호 — 싱글 값은 auto_all phase 12 에서 참고로 기록 |
| F21-11 | (멀티 맵) | perma_sprite·CGRPPainter 를 매 프레임 쓰는 2인 이상 맵에서 디싱크 없음 |

**UE_RE 이식판** (1)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| S0-14 | UE_RE_S0.scx (①) | LAN 2명 5~10분 디싱크 없음, 두 사람 모두 불러오기 |

**theSeed 이식판** (5)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| T0-14 | theSeed_eud_S0_t1 (2인) | 풀어도 곧바로 다시 동맹·시야 공유 |
| T0-15 | theSeed_eud_S0_t1 (2인) | 두 화면 같은 줄, 동시 누름이면 한 줄·두 횟수 +1 |
| T0-16 | theSeed_eud_S0_t1 (2인) | 두 PC 값이 같음 |
| T0-17 | theSeed_eud_S0_t1 (2인) | 30분 드랍 없음, 끝에 두 화면 확인 줄 같음 |
| T0-18 | theSeed_eud_S0_t1 (2인+관전자) | 관전자 채팅이 전체로 감, 관전자에게도 확인 줄 |

**Respect V 이식판** (8)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| S1-4-3 | Respect_V_eud_S1.scx (LAN 방) (2인) | P2 패배·강퇴 문구, P1 화면에 완료 문구, 디싱크 없음 |
| S1-4-4 | Respect_V_eud_S1.scx (LAN 방) (2인) | 인식 전·칭호 없는 사람의 @파랑나가 는 아무 일 없음 |
| S1-5-1 | Respect_V_eud_S1.scx (LAN 방) (2인) | 120초 안·난이도 전 방장 유닛 → 로케이션 49: 공유 켜짐, 문구 한 번, TAdUpd05(귀) |
| S1-5-3 | Respect_V_eud_S1.scx (LAN 방) | 한 사람이 쓰면 모든 사람 광물이 같은 값으로 움직임, '벌었음/사용함' 고정 줄 — 1인 판에서도 '사용함' 줄 값은 자동 확인 가능 |
| S1-6-5 | Respect_V_eud_S1.scx (LAN 방) (관전자) | 관전자 PC 에서도 곡이 들림, 끄기 버튼 없음 |
| S1-7-4 | Respect_V_eud_S1.scx (LAN 방) (2인) | 방장이 나가면 다음 앞 슬롯 사람이 방장(선택 유닛·62·63 권한) |
| S1-8-3 | Respect_V_eud_S1.scx (LAN 방) (2인) | 사람이 모두 나가면 P6~P8 패배 — 나간 뒤라 브리지로 볼 수 없음 |
| S1-8-4 | Respect_V_eud_S1.scx (LAN 방) (5인) | 5인 5분 끊김·디싱크·튕김 없음, 무거우면 기록(프레임/초) |

**Memory 2 이식판** (3)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| M2-S0-20 | Mem2_S0_eudext.scx (2인) | 5~10분 동기화 오류 없음 |
| M2-S0-21 | Mem2_S0_eudext.scx (2인) | 방장 아닌 PC 에서도 같은 빠르기(Dt 는 방장 값) |
| M2-S0-22 | Mem2_S0_eudext.scx (2인) | QC 유닛(58) 이 보이지 않고 선택되지 않음, 선택·부대 지정 유지(선택 표에 QC 유닛 없음 — 자동) |

**Memory 1 이식판** (2)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| M1-S0-14 | Mem1_S0_A.scx (2인) | 10분 동기화 오류 없음, 두 사람 F12 눌러 보기에도 이상 없음 |
| M1-S0-17 | Mem1_S0_B.scx (2인) | 5분 디싱크 없음 |

## 6. 튕길 위험이 있는 것 — 따로, 마지막에 (34)

다른 확인이 끝난 뒤에 하십시오. 멈추거나 튕길 수 있습니다(일부러 그러는 항목도 있습니다).

**eudext 확인 맵 — 필수** (9)

| 번호 | 맵·판 | 무엇을 보는지 | 위험 |
|---|---|---|---|
| D14-3 **필수** | 10 bullet_check | bullet=0, bullet_to=0, bullet_at=0 (zeroShots 1), 그 뒤 seq 계속. 채운 수 기록(기준 맵이면 1634 근처) | 유닛 칸 1,700 가득 + 탄 생성 시도 |
| F19-4 **필수** | 17 exit_trap_check | P1 이 나갈 때 P1 의 스타가 멈춤(사람), P2 PC 는 seq 계속(자동) | 실험 기능 — 로컬 트리거 목록을 자기 순환으로 바꿈. 목록 주인을 잘못 고르면 전원 멈춤, 강제 종료 필요 |
| F21-1 **필수** | 18 sprite_check | 만듦 16 실패 0, bounceE ≠ 0 (자동). 점이 보이고 안 움직이고 20초 뒤에도 남음, 높이별 겹침, 속도 1280 스프라이트 움직임은 눈 | 18 맵 전체가 [unlimiter] + 영구 스프라이트 + (53초) 유닛 칸 가득 채우기 |
| F21-2 **필수** | 18 sprite_check | epd ≠ 0 둘 다(자동), 스톰 모양·수명 30/60 차이(눈) | 18 맵 전체가 [unlimiter] + 영구 스프라이트 + (53초) 유닛 칸 가득 채우기 |
| F21-3 **필수** | 18 sprite_check | +0xDC 0x100 켜짐·+0xE4 0, 약 96프레임 뒤 칸 비움, 드래그 선택에 안 잡힘(선택 표 자동), 2100 레이스는 잡힘, 움직이는 레이스 61번. 클로킹 소리·끊김 없는 움직임은 눈·귀 — 사람 몫: 드래그·클릭 | 18 맵 전체가 [unlimiter] + 영구 스프라이트 + (53초) 유닛 칸 가득 채우기 |
| F21-4 **필수** | 18 sprite_check | 남은 스캐너 유닛 1(마지막 remove=False 만) — 자동. 스캔 모양은 눈 | 18 맵 전체가 [unlimiter] + 영구 스프라이트 + (53초) 유닛 칸 가득 채우기 |
| F21-5 **필수** | 18 sprite_check | epd ≠ 0, 마린이 (3413, 3413) 근처(±64)로 옮겨졌으면 '끌려옴' 기록(자동). 리콜 모양은 눈 | 18 맵 전체가 [unlimiter] + 영구 스프라이트 + (53초) 유닛 칸 가득 채우기 |
| F21-6 **필수** | 18 sprite_check | 다 그림 1, 점 120/120, 실패 0 (자동). 색·모양은 눈(사진) | 18 맵 전체가 [unlimiter] + 영구 스프라이트 + (53초) 유닛 칸 가득 채우기 |
| F21-7 **필수** | 18 sprite_check | 헤더 값 = CS_Photo 입력값(자동 대조 — 입력값을 사람이 알려 줘야 함), 다 그림 1·실패 0, 그림이 원본 BMP 와 같음(눈) — CS_Photo 로 만든 sprite_user.cgrp + 다시 빌드 필요 | 18 맵 전체가 [unlimiter] + 영구 스프라이트 + (53초) 유닛 칸 가득 채우기 |

**eudext 확인 맵 — 권장** (8)

| 번호 | 맵·판 | 무엇을 보는지 | 위험 |
|---|---|---|---|
| B10-9 | 03 units_ingame | 끊김 체감 — 브리지는 F30 프레임 시간(ms)·F30~F40 평균 프레임/초를 기록 | 유닛 한도 채우기와 같은 프레임 |
| B15-8 | 04 datpatch_check (변형) | images grpFile·overlay 쓰기 뒤 오버레이가 바뀌는지(눈), 오류·튕김 없음(브리지) | 읽기 전용 표기 칸(그림 포인터 계열) 쓰기 — 그릴 때 튕길 수 있음 |
| D14-10 | (대량 패턴 맵) | 소유자 사람/컴퓨터별 시야·총알 약 4096 표시 한도 | 총알·스프라이트 표 한도 넘김 |
| D14-11 | (대량 패턴 맵) | [unlimiter] 켬/끔에서 한 프레임 80발 이상 | 언리미터 끔 + 대량 탄 |
| D16-5 | 11 bgm_check | ⑦ 없는 파일 PlayWAV·⑧ 없는 알림 뒤 게임이 멈추지 않음(seq 계속), 오류 창 없음, ① 음이 끝까지(귀) | MPQ 에 없는 파일 재생 — SC:R 동작 미확인 |
| F21-8 | (28장 함수를 쓰는 맵) | 진짜 벌처·스캐너가 있는 맵에서 모양·스캔 크기가 깨지는가(문서화용) | 이미지 256 iscript 86·스캐너 크기 0 영구 부작용 |
| F21-9 | 18 sprite_check | storm·perma·unit_sprite·recall 모두 0, fullZero 1, 그 뒤 seq 계속 | 유닛 칸 가득 + 스프라이트 생성(언리미터 맵) |
| F21-10 | (언리미터 뺀 변형) | [unlimiter] 없이 28장 함수·CGRP 가 어떻게 되는지, 한 프레임 점 수 한도 | 가이드북상 CGRP 는 언리미터 필수 — 튕길 가능성 높음 |

**UE_RE 이식판** (3)

| 번호 | 맵·판 | 무엇을 보는지 | 위험 |
|---|---|---|---|
| S0-1 | UE_RE_S0.scx (①) | 시그니처가 나타나고 seq·게임 프레임이 계속 늘어남(자동 감시). 오프닝 글 스크롤·카운트다운은 눈 — 가장 중요 — 사용자가 직접 보고, 브리지는 감시 기록만 | STRCtrig 런타임 재배치가 0.81 페이로드에서 맞는지 미확인(가장 큰 미확인) — 틀리면 시작 직후 멈춤·튕김 |
| S0-16 | UE_RE_S0_cplp_out.scx (②) | 배포판 _out 처럼 열리고 S0-1~3 이 같음 | CPLP 보호 검사가 0.11 출력(가짜 scenario.chk 두 벌)에서 오작동하면 시작 직후 멈춤 |
| S0-19 | UE_RE_S0_eudext.scx (③) | S0-1·S0-3·S0-12 가 ①과 같음 | eudext 부트를 얹은 하이브리드의 첫 시작(STRCtrig 재배치 + eudext 페이로드) |

**theSeed 이식판** (4)

| 번호 | 맵·판 | 무엇을 보는지 | 위험 |
|---|---|---|---|
| T0-2 | theSeed_eud_S0_t1 (다른 닉네임) | 가운데 두 줄 + 패배 + EUD ERROR 로 멈춤(원본과 같음) — 멈춘 뒤 seq 정지로 기록 — 다른 계정 필요(없으면 건너뜀) | 일부러 멈추기(unsafe_halt) — 강제 종료가 필요할 수 있음 |
| T0-4 | theSeed_eud_S0_t1 (방 배속 옵션) | '방 제목에서 배속 옵션을 제거해 주십시오…' 세 줄 + 패배 + 멈춤 | 일부러 멈추기 |
| T0-5 | theSeed_eud_S0_t1 (슬롯 변경) | 사람/컴퓨터 슬롯·종족 변경 각각의 문구 + 패배 + 멈춤 | 일부러 멈추기(3판) |
| T0-19 | theSeed_eud_S0_t1 (2인, 닉네임 없음) | 두 PC 모두 같은 문구 + 패배 + 멈춤(디싱크 창 아님) | 일부러 멈추기(두 PC) |

**Respect V 이식판** (5)

| 번호 | 맵·판 | 무엇을 보는지 | 위험 |
|---|---|---|---|
| S1-3-1 | Respect_V_eud_S1.scx (LAN 방) (방 제목 배속 옵… | 실행 방지 문구 → 패배 → 튕김 | 일부러 튕기기(실행 방지) — 원본의 ExitDrop 은 없지만 패배 뒤 튕김 |
| S1-3-2 | Respect_V_eud_S1.scx (LAN 방) (사람 슬롯에 컴퓨… | '사람 슬롯 변경…' → 튕김 | 일부러 튕기기(실행 방지) — 원본의 ExitDrop 은 없지만 패배 뒤 튕김 |
| S1-3-3 | Respect_V_eud_S1.scx (LAN 방) (컴퓨터 슬롯·종족… | '컴퓨터 슬롯 변경…' / '컴퓨터 종족 변경…' → 튕김 | 일부러 튕기기(실행 방지) — 원본의 ExitDrop 은 없지만 패배 뒤 튕김 |
| S1-3-4 | Respect_V_eud_S1.scx (LAN 방) (싱글플레이) | '싱글플레이로는 플레이할 수 없습니다…' → 튕김 | 일부러 튕기기(실행 방지) — 원본의 ExitDrop 은 없지만 패배 뒤 튕김 |
| S1-3-6 | Respect_V_eud_S1_T1.scx (허용 닉네임 없음) | '테스트 전용 맵입니다…' → 패배 → 튕김 — 다른 계정 필요 | 일부러 튕기기(실행 방지) — 원본의 ExitDrop 은 없지만 패배 뒤 튕김 |

**Memory 2 이식판** (3)

| 번호 | 맵·판 | 무엇을 보는지 | 위험 |
|---|---|---|---|
| M2-S0-1 | Mem2_S0_eudext.scx | 로딩·시작에서 튕기지 않음, seq 계속, freeze 문구 없음 | STRCtrig 런타임 재배치(0.81) 미확인 — 틀리면 시작 직후 멈춤·튕김 |
| M2-S0-11 | Mem2_S0_eudext.scx | O·P·F12·TAB·INSERT·DELETE·ESC 를 눌러도 멈춤 없음, 테스트 기능 관련 데스·스위치 변화 없음(출시판) | 0.81 에서 고친 NSQC 키 기억 칸 — 틀리면 배열 밖 쓰기(메모리 오염·디싱크·멈춤) |
| M2-S0-14 | Mem2_S0_eudext_cplp_out.scx | 보호판이 똑같이 불러와지고 확인 줄 한 번 | CPLP 검사 + STRCtrig 재배치 첫 시작 |

**Memory 1 이식판** (2)

| 번호 | 맵·판 | 무엇을 보는지 | 위험 |
|---|---|---|---|
| M1-S0-1 | Mem1_S0_A.scx | 제목 '마린키우기 Memory v.4.0R', 시작 직후 멈춤 없음, 약 3초 뒤 인트로 글·BGM — 가장 중요. A 판은 블록이 없어 감시하려면 변형이 필요 | STRCtrig 런타임 재배치(0.81) 미확인 — 틀리면 시작 직후 멈춤·튕김 |
| M1-S0-19 | Mem1_S0_C.scx | 출시판 _out 처럼 열리고 M1-S0-1·4 가 같음 | CPLP 검사가 0.11 출력(가짜 scenario.chk)에서 오작동하면 멈춤 |

## 7. 다른 프로그램이 필요한 것 (17)

SCMDraft 2 + TEP, SCR_DB 런처, CS_Photo 등이 필요합니다. C9-1·D7-6 절차는 확인 문서 2절에 그대로 있습니다.

**eudext 확인 맵 — 필수** (4)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| D18-1 **필수** | 14 scrdb_check | 표지 Seq 증가, LocalPlayer = 1, 로그 '연결되었습니다 … 빌드 N, P1' → '매니페스트 : …eudext_scrdb_check.json (빌드 일치, 항목 5개)'. 알림음은 귀 — 런처 실행은 사람 |
| D18-2 **필수** | 14 scrdb_check | loaded=1, Gold 0 Level 0 Big 0x12345678 Stat 7 Flags 0xABCDEF01, 받은 워드 P1 ≥ 1, 로그 '저장된 파일이 없습니다' |
| D18-3 **필수** | 14 scrdb_check | F8 뒤 saves=1, Level 1, 로그 '저장 시작 … 저장 완료. (5개 항목)', 저장 완료 표식(0xFFFD) 처리 — 사람 몫: F8 |
| D18-4 **필수** | 14 scrdb_check | 다시 연 판에서 저장했던 Gold·Level·Big 0x12345678·Stat 7·Flags 0xABCDEF01 (상위 16비트 포함), 로그 '불러오기 완료', '어긋난 항목' 없음. 알림음은 귀 — 사람 몫: 맵 나갔다 다시 열기 |

**eudext 확인 맵 — 권장** (6)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| B15-2 | 04 datpatch_check | SCMDraft 로 CHK 를 바꾼 맵과 트리거 패치 맵의 두 칸 값이 같음 |
| B15-9 | (M2 from_eud_editor 빌드) | from_eud_editor 로 만든 M2 와 원래 DataEditor.py 맵의 dat 칸이 같음 — 덤프를 자동 대조 |
| D18-5 | 14 scrdb_check | 런처 끄기 → 끊김 소리, 매니페스트 치우고 연결 → 오류 소리 (이 PC 만) |
| D18-7 | 14 scrdb_check (다시 빌드) | 빌드 ID 가 바뀐 맵 + C:\Temp 매니페스트 삭제 → 로그 '매니페스트 : … (항목 지문 일치 …)' |
| D18-8 | 14 scrdb_check | 런처 --dev 'MSQC 채널 시험' 오류율·도착률이 DPS 맵과 비슷 |
| D18-9 | (빌드 로그) | 'Sendable value range for val syntax: 0 to 4194303', 64×64 미만 맵은 빌드 오류 |

**SCMDraft 확인** (2)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| C9-1 | (게임 없음) mathx_tep_check.py | 'C9-1 일치' — 게임을 열지 않는다 |
| D7-6 | (SCMDraft + TEP 맵) | 혼자 열면 'D7-6: 47 95 (eudext 와 같음)' |

**UE_RE 이식판** (4)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| S0-3 | UE_RE_S0.scx (①) | 표지 Seq 증가, 불러오기 완료 신호, 로그 '불러오기 완료'. 레벨·스탯·칭호 복원·GUEST 사라짐·소리는 눈·귀 — 런처 실행은 사람(P-1 백업 먼저) |
| S0-12 | UE_RE_S0.scx (①) | HOME 뒤 로그 '저장 완료', 다시 들어가 불러오면 같은 값(표지·로그 자동), 문구는 눈 — 사람 몫: HOME, 다시 열기 |
| S0-13 | UE_RE_S0.scx (①) | 게임 시작 뒤 켠 런처로는 불러오기가 되지 않음(표지 상태·로그) |
| S0-17 | UE_RE_S0_cplp_out.scx (②) | SCMDraft 로 열리지 않음 |

**Memory 1 이식판** (1)

| 번호 | 맵·판 | 무엇을 보는지 |
|---|---|---|
| M1-S0-20 | Mem1_S0_C.scx | SCMDraft 로 열리지 않음 |

## 8. 정해 주실 것 (답만 적어 주시면 됩니다)

| # | 질문 | Claude 권장 | 답 |
|---|---|---|---|
| Q-1 | eudext 수정 두 브랜치(`fix/windows-lua-locale` → `fix/shape-crmath`: Windows 로캘 수정, 도형 정확 반올림)를 main 에 합치고 푸시할까요? | 합치기 (시험 통과, Ubuntu 쪽 작업과 겹치지 않게 먼저 푸시) | |
| Q-2 | MapSource `fix/snqc-081`(SNQC 1.3: 0.81 키 배열 밖 쓰기, 합치기 판정 wrap, EUDVArray 오류 수정)을 master 에 합치고 푸시할까요? 합치면 `C:\euddraft0.9.2.0\plugins\SNQC.py` 교체 → DPS·theSeed 다시 빌드 → 배틀넷 2인 확인 | 합치기 (인게임 확인은 합친 뒤) | |
| Q-3 | eudext 인게임 확인(1절)을 맵 이식보다 먼저 할까요? | 먼저 (모듈 가정이 틀리면 이식판 전체가 흔들림) | |
| Q-4 | eudext 설계 결정 대기 항목(`eudext\eudext\DESIGN.md` 7절 D3, D13~D97 — 대부분 권장안으로 이미 구현)을 확인해 주실까요? | 시간 날 때 | |
| Q-5 | UE_RE: 하이브리드(TEP 트리거와 eudext 를 한 맵에 두고 하나씩 교체)로 계속할까요? | 예 (S0-1 이 실패하면 다시 판단) | |
| Q-6 | UE_RE: 원본 버그를 보존할까요? — CreateBullet 이 무기 번호 대신 유닛 번호로 색인, 상점 `AtkUpCompCount[2]`, OneClickUpgrade 0→1 무료, 보스 몇 건 | 1차는 보존, 고칠 것은 따로 표시 | |
| Q-7 | UE_RE: 휴면 Sans 보스를 옮길까요? 테스트·치트 코드를 옮길까요? 난수 수열이 원본과 달라져도 될까요? | Sans 는 나중, 치트는 테스트판에만, 난수 달라짐 허용 | |
| Q-8 | theSeed: 방식(로직은 eudext 로 새로, MapConfig 는 lupa 로 읽기)으로 계속할까요? 0.6 시드와 같은 배치를 계속 재현해야 하나요? | 예 / 재현 필요 여부는 답 필요 | |
| Q-9 | theSeed: SNQC 를 계속 쓸까요(그러면 eudext 에 SNQC 전송 추가)? CPLP 와 freeze 중 무엇을 쓸까요? | SNQC 유지 + eudext 에 추가 / freeze 우선 | |
| Q-10 | theSeed: 원본 동작 중 고칠 것·보존할 것 — 최후 건물 2000틱, 효과음 +1, energy 의존. 승인 닉네임 접두 일치 버그는 이식판에서 고쳤습니다(괜찮은지) | 보존, 닉네임 수정은 유지 | |
| Q-11 | theSeed: MSF 공용 eudext 틀을 어디에 둘까요? 이식 기간에도 TEP 판을 계속 출시하나요? 디버그 도구도 옮길까요? | 틀은 MSF-Template 저장소 / 답 필요 / 나중 | |
| Q-12 | Respect V: 새로 짜기 방식으로 괜찮은지, 원본 버그 B1(공유 본체 `PreserveFlag=1` 소환 413곳이 같은 종류 둘째 건물부터 패턴이 안 나옴)을 재현할지 고칠지 | 새로 짜기 / 1차 재현 | |
| Q-13 | Respect V: 무작위 정렬 소환 86곳을 "컴파일 때 만든 변형 몇 개 중 실행 중 선택" 으로 근사해도 될까요(eudext D14)? 소환 → 유닛 생성 대기열 통로를 eudext 에 넣을까요? | 근사 허용 / eudext 에 넣기 | |
| Q-14 | Respect V: MSQC 출력을 데스값 그대로 둘지 배열로 바꿀지, ExitDrop·APMCounter 를 유지할지 | 데스값 유지 / 크래시 쓰기만 유지, APMCounter 유지 | |
| Q-15 | Respect V master 에도 안 쓰는 CA2Arr 제거(트리거 약 5,100개 절감)와 `CB_initTCopy` 수정을 따로 넣을까요? | 예 (MSF-Template 480fc8e 와 같은 수정) | |
| Q-16 | DPS 이식판에서 찾은 버그 두 건 — `eud/ctrig/luart.py:181` `__Encode_cp949` 가 변환을 안 함(stat_txt 표 크기 다름), `GetiTblId` 가 오류 없이 0 — 을 DPS 작업 쪽에서 고칠까요? | 예 (DPS 작업하시는 곳에서) | |
| Q-17 | Memory 1 섹션 나누기(`refactor/memory1-split`)를 MapSource master 에 합칠까요? 동작은 같고 파일만 나뉩니다 | 합치기 | |
| Q-18 | **지금 Library 로는 master 의 Memory 1 이 컴파일되지 않습니다.** Library 커밋 6135017 이 `TMemoryX` 에 nil 검사를 넣었는데 `TCDeaths` 가 Mask 없이 불러 GunTrig 에서 멈춥니다(이식판은 컴파일 때 우회). 출시판 4.0R 은 Library d85db5d(2024-10)로 만들어졌습니다. Library 를 고칠까요? | `TMemoryX` 가 Mask 없음(nil)을 받게 고치기 — 다른 맵 빌드 결과가 안 바뀌는지 확인하고 | |
| Q-19 | Memory 1 의 최신 입력 맵(EUD Editor `MSF_Memory.e3s` 가 가리키는 `MSF_Memory_New2_out.scx`)이 이 PC 에 없습니다. 어디 있나요? 없다면 지금 기준 맵(2022 `MSF_Memory_New.scx` + 출시판 음원·제목)을 정본으로 할까요? | 찾으면 그 판으로, 없으면 지금 기준 맵을 정본으로 | |
| Q-20 | Respect V: HEAD 판 입력 맵(로케이션 58~63 이 있는 `Respect V_BGM.scx`)과 새 음원 폴더(52번 곡 `BGM_CottonCandySoda2.ogg` 포함)가 이 PC 에 없습니다(`C:\euddraft0.9.2.0` 에는 2025-11 판). 어디 있나요? | 옮겨 주시면 그 판으로 다시 빌드 | |
| Q-21 | Respect V: 입력(MSQC) 결과를 데스값 대신 eudext sync 펄스로 받게 했습니다(평가 때 권장안과 다름, RV15). 괜찮을까요? | 괜찮음 (이식판 안에서만 쓰는 값) | |
| Q-22 | Memory 2: 하이브리드 점진 이전(UE_RE 와 같은 방식)으로 계속할까요? 입력은 NSQC 를 0.81 에 맞게 고친 사본으로 둘까요(SNQC·MSQC 로 바꾸는 대신)? | 예 / NSQC 사본 유지 | |
| Q-23 | Memory 2: 원본 버그를 보존할까요(평가 보고서의 목록)? Axiom 2~4 개발은 이식 중에도 Lua(TEP 판)에서 계속하시나요? | 1차 보존 / Lua 에서 계속하면 이식판이 따라감 | |
| Q-24 | Memory 2: 고정밀 lengthdir 표(11.9MB)와 unlimiter(8.8MB)가 꼭 필요한가요? 빼면 용량이 크게 줄어듭니다(순수 eudext 추정 33~46MB → 21~34MB) | 필요한 곳을 확인한 뒤 결정 | |
| Q-25 | Memory 2: 최신 SCMDraft 원본 맵은 어디 있나요? 공용 `C:\euddraft0.9.2.0\MSF_Memory2.scx`(2024-02)는 저장소 기본 맵(0.ZI_Fix 입력)보다 옛 판입니다 | 위치를 알려 주시면 그 판으로 맞춤 | |
| Q-26 | Memory 1: 이식을 어느 Library 판으로 이어 갈까요(출시판 d85db5d / 지금 판)? 5.3 의 ①·② 결과를 보고 정하시면 됩니다 | ①·② 가 같으면 지금 판 | |
| Q-27 | Memory 1: 원본 버그 19건(F1~F19 — GunTrig 1417 CP 누수, `button3.wav` 없음 등)을 어떻게 할까요? 이식과 별개로 master 에 먼저 넣을 만한 안전한 수정(F1, F15, Q-18)도 있습니다 | 게임 결과가 바뀌는 것은 보존, CP·메모리 오염과 표시·소리는 고침 / 안전한 수정은 master 에 따로 | |
| Q-28 | Memory 1: 방식(하이브리드), 입력(NSQC 0.81 사본 유지), 테스트·치트 코드(테스터 권한 경로만, 만료일은 고정값), 난수 수열이 달라지는 것 — 권장안대로 괜찮을까요? | 예 | |
| Q-29 | UE_RE 기준 맵을 트리거 없는 판으로 바꿀까요? 컴파일기가 트리거 칸을 어차피 전부 새로 쓰는 것이 확인돼 산출물은 같습니다 (지금은 안 바꾸고 그대로 뒀습니다) |

## 9. 자리를 비우셨다가 돌아오시면

- 이 문서 2절(자동 판정)에 RUN.bat 경로와 맵 순서가 적혀 있으면 그것부터 하시면 됩니다.
- Claude 쪽 진행은 `MapSource\EUDPLIB_PORT_CHECKLIST.md` 맨 앞 "검증 상태" 표에서 볼 수 있습니다.
- 8절 질문 중 **Q-1·Q-2**(브랜치 병합)와 **Q-19·Q-20·Q-25**(최신 입력 맵·음원 위치)가 가장 막혀 있는 것입니다.
