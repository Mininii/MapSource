# SNQC 작업 내역

2026-09-16 ~ 2026-09-17, Claude 데스크톱 세션. 제작자와 함께 theSeed(마린키우기 theSeed) 테스트 빌드로 실측하며 진행.
자세한 실측 기록은 theSeed 저장소의 `QUEUE_COMMAND_RESEARCH.md`, `PROGRESS.md` §41·§42 에 있다.
기록 파일(CSV)은 그 PC 의 `C:\Temp\rtreview\` 에 남겼다 (저장소에는 없음).

## 1. 출발점 - MSQC 입력이 가끔 빈다

- theSeed 인게임 런타임 리뷰(`RUNTIME_REVIEW_HANDOFF.md` A1-6)에서 커스텀 설정 슬라이더를 끄는 도중 입력이 한 사이클 통째로 비었다.
- 제작자 요청: "MSQC 를 분석하고, 새로운 전송 방식을 만들면서 큐 커맨드를 탐구한다."
  제작자 제안: "건물의 랠리 포인트로 큐 커맨드를 구현할 수 있지 않을까?"

## 2. 턴 버퍼 실측 (디버그 브리지 + `diag/dbg_framelog.py`)

- 매 사이클 턴 버퍼 길이(0x654AA0)·앞 48바이트, 상한(0x57F0D8 = 496), 지연(0x6556E4)을 기록.
- 싱글·LAN 모두 **매 프레임 턴**이 나가고 명령 지연은 **3사이클**. 매 턴 맨 앞에 게임이 넣는 Sync(0x37) 7바이트가 있다.
- MSQC 패킷 = `09 01 QC` + `15 x x y y 00 00 E4 00 06 00` (Move). 키 비트는 기준 (64,64) 에 더해진다.
- **원인 발견**: 게임 프레임 % 150 = 8 인 사이클마다 받는 쪽 값이 빈다 (LAN 150초에 34번, 두 플레이어 모두).
  그 순간 QC 유닛의 오더 상태(+0x4E)가 0 (78번 중 77번) → **스타가 150프레임마다 QC 유닛의 Move 를 다시 시작시킨다.**
- 곁가지: CUnit+0x74(오더 큐 머리)를 감시하려다 EUD ERROR 0xFF9E0813 (= ~0x0061F7EC). SC:R 이 막는 칸.

## 3. 랠리 채널 실험 (theSeed `MapLogic/QueueCommandLab.lua`, 테스트 빌드 전용)

| 차수 | 한 것 | 결과 |
| --- | --- | --- |
| 1차 | 고른 유닛에 맵 트리거가 턴 버퍼로 직접 Select + 오더 40 을 쓴다 (Alt+8) | 명령이 그대로 실행된다. 배럭은 3사이클 뒤 랠리 좌표가 바뀌고 **값이 그대로 남는다**(7600사이클). 맵 밖 좌표·컴샛은 무시 |
| 소스 확인 | OpenBW(`actions.h`, `bwgame.h`)·BWAPI 소스 | 랠리는 **12종**·땅에 앉은 건물·소유자 바이트만 본다, 오더 39 는 대상 유닛만 싣는다, Select 는 숨김 스프라이트를 거른다 |
| 2차 | 여러 종류 건물 10개를 만들어(Alt+9) 차례로 전송 | 12종 중 땅에 앉은 것만 받음, 떠 있으면 무시. 오더 39·틀린 ID·맵 밖 규칙이 소스와 일치. **P12 에 넘긴 뒤 소유자 바이트만 되돌린 건물도 받음** |
| 곁가지 | 유닛 속성(UPRP) 무적·띄움 | SC:R 에서 기대대로 안 먹음 (무적 → 건물이 뜸) → 무적은 +0xDC 직접 쓰기로 |
| 곁가지 | 스프라이트·이미지 숨김 플래그 | 효과 없음 - 이 방식으로 읽은 CUnit+0x0C 가 0 이었다 (원인 미확정) |
| 3차 | 제작자 기억 "건설크기 0,1 / 1,0 / 0,0" + MSQC.py 가 QC 유닛에 (0,0) 을 씀 | **세 크기 모두 건물이 안 보이고 드래그로도 안 골라지는데 명령은 받는다** |
| 4차 | 가린 커맨드센터 + 받는 쪽이 매 사이클 랠리 칸을 (0,0) 으로 되돌림 (공유 트리거) | 받은 횟수가 예상과 정확히 같음 (두 번 돌려 9 → 18). 같은 값 반복·연속 4사이클도 빠짐없이 받음 |

실험 중 테스트 빌드에 넣은 것: Alt+9 실험 건물, Alt+8 전송, Alt+0 "웨이브 끄기" (실험 건물이 적에게 부서지지 않게).

## 4. 채널 건물 종류 결정 (제작자)

- 12종 모두 theSeed 에서 적 건작 등으로 쓰이고 있었다. 건설크기는 종류 단위라 같은 종류의 적 건물까지 가려진다.
- 제작자 결정: **커맨드센터(106) 를 채널 전용으로 비운다.** 2티어 "커맨드센터" 건작은 사이언스 퍼실리티(116) 가
  커맨드센터 외형(flingy 94)·와이어프레임·이름·파괴 패턴·BGM 을 물려받아 대신 서고, 116 은 잡건작에서 뺐다
  (theSeed `MapLogic/BuildingAppearance.lua`, PROGRESS §42). 인게임에서 구별되지 않음을 확인.

## 5. 구현 (2026-09-17)

제작자 요청: "DPS강화하기의 eudplib 판과 기존(CtrigAsm) 판 중 무엇이 나을지 실측 전이라 둘 다 필요" → 두 판을 만들었다.

- **`SNQC.py`** - MSQC.py(eudplib 0.76.14 에서 동작)의 설정 해석부를 그대로 가져와 줄 문법을 맞추고, 채널을 랠리 방식으로 바꿨다.
  - 채널 건물 생성(사람 소유 → P11 에 넘김 → 소유자 바이트 되돌림 → 무적·충돌 없음), 없어지면 다시 만들기(`QCDebug`).
  - 키 비트 배치를 맵 크기에 맞춰 넓힘 (192×96 에서 21 → 23비트, 256×256 에서 26비트). 값은 x 에 1 을 더해 기준값 (0,0) 을 피한다.
  - 합치기(Merge): 아직 안 나간 자기 패킷이 버퍼에 있으면 좌표만 고친다 (키 = 비트 합치기).
  - 선택 되돌리기: eudplib 의 `QueueGameCommand_Select`, 아무것도 안 고른 경우 `QueueGameCommand_RemoveSelect` 로 채널을 뺀다.
  - `QCUnit` 이 12종이 아니면(보통 그렇다) 무시하고 106 을 쓴다 → .eds 는 단락 이름만 바꾸면 된다.
- **`SNQC.lua`** - 같은 설계를 CtrigAsm 으로. Lua 함수 설정 + MSQC 줄 문법(`SNQC_Line`). 값 유지(Hold)·새 값 표시(NewDeath)·
  바뀐 값만 보내기(Change) 를 더 넣었다. 작업 공간 주소(`WorkAddr`) 설정 - DPS 는 0x58F500~ 를 CreateVoid 없이 직접 쓴다.
- 시험하며 고친 것: 점검 카운터가 지역 변수로 잡힘, `EUDElse()()` 사용법, 로케이션 번호 기준(eudplib 은 1부터),
  Lua 5.3 의 `2^n` 실수 문제, TEP 의 "정의 안 된 전역 읽기 = 에러"(`loadstring`), 설정 이름 검사.

## 6. 시험

| 시험 | 결과 |
| --- | --- |
| 오프라인 컴파일 (`tests/test_snqc.py`) | theSeed 설정(7명 × 9채널), DPS_eud 설정(4명 × 11채널, SCR_DB 채널 8개 포함) 통과 |
| Lua 가짜 환경 (`tests/stub_test.lua`) | 41줄 설치 끝까지 실행, 블록 짝 정상 |
| **theSeed 싱글** (eds `[SNQC]`, `QCPlugin = "SNQC"`) | 채널 건물 안 보임·사람 유닛 수 0, 키(Alt+숫자·설정 키)·설명서·선택 정상, **150프레임 소실 없음** (11999사이클). MSQC 의 "아무것도 안 고른 채 부대지정하면 QC 유닛이 선택되던" 문제도 사라짐 |
| **theSeed LAN 2인** (64비트 + 32비트) | **디싱크 없음**, 두 플레이어 값 모두 받음, 조작감 정상. 20940사이클(150프레임 주기 139번) 중 P2 에 1사이클 빈칸 2번 (한 턴에 두 사이클이 실린 것으로 봄), P2 가 나갈 때 P1 에 1번 |

## 7. 남은 일

- 플러그인 판에도 값 유지(Hold)·새 값 표시(NewDeath) - 한 턴에 여러 사이클이 실린 경우의 빈칸 대비 (theSeed 슬라이더 드래그).
- 합치기 실측 (턴이 여러 사이클에 한 번 나가는 방), 버퍼 한계.
- ~~CtrigAsm 판 실제 TEP 컴파일~~ (8절, MSF_UE_RE) · 인게임.
- DPS 적용 (DPS_eud: `[MSQC]` → `[SNQC]`, EUD Editor 3 dat 에서 106 을 바꿨는지 확인).

## 8. CtrigAsm 판을 MSF_UE_RE 에 적용 (2026-09-17, 1.1)

제작자 요청: "SNQC 를 검증해 보고 싶다. 마린키우기 MSF_UE_RE 에 Lua 판으로 적용."

**맵 조사**
- 106(커맨드센터): 코드·버튼셋·맵 UNIT/TRIG 어디에도 없음. 요구사항 표(원본 값)와 맵 UNIx 사용자 설정(HP 1500 등, 쓰이지 않음)만 있다.
- 걸리는 곳 3가지: ① `EUDinit.lua` 의 게임 시작 3초 루프가 모든 건물의 건설크기를 1x1 로 덮고, 미리 놓인 유닛(주인 바이트 ≤ 7)을
  기록해 0~227번을 전부 RemoveUnit 한 뒤 다시 만든다 - 58(MSQC QC 유닛)만 건너뛰었다. ② 보스 클리어 3곳의 `KillUnit("Any unit", P11)`.
  ③ 같은 3초에 0x58F448~0x5967E8 을 한 번 지우고, 보스마다 0x594000~ 를 지운다(작업 공간 자리).
- P11 에 넘기는 유닛: Terran Wraith / Tom Kazansky (DemonLanterns·DemonicEmperor), 60 / Gantrithor (DemonicEmperor), 94 / 193 (Destr0yer).
  Identity 는 없음. 미리 놓인 P11 유닛도 없음.
- 채널 자리: 타일셋 자료가 없어 미리 놓인 유닛(최소 7타일)과 코드가 쓰는 로케이션을 피해 (2512, 3312) 로 골랐다.
- MSQC 줄 중 4줄이 결과를 EUDArray(`VChatIndex`)로 받았다 → 데스 191 로 바꾸고 main.eps 가 데스를 읽게 했다 (세 방식 공통).

**실제 컴파일에서 나온 것** (가짜 환경에서는 안 보였다)
1. `need_include_datatransfer` - `f_Read` 가 `Include_CtrigPlib` 를 요구 → 3초 CIf 안 init 블록 뒤에서 설치.
2. `IndexAllocation_Overflow` - 계측해 보니 SNQC 가 인덱스 6990개(맵 나머지 약 5300개, 한도 12288). 설치부를 SetCall + CTrigger 로 다시 짜서 112개 (다시 계측).
3. 코드를 읽다 찾은 것: `Deaths(CurrentPlayer, ...)` 가 FP 기준으로 평가됨, `mouse` 로케이션 번호 하나 밀림, 작업 공간이 지워지면 패킷이 깨짐,
   생성 실패를 성공으로 볼 수 있음, 죽은 채널을 산 것으로 봄. 모두 1.1 에서 고침 (`CHANGELOG.md`).

**시험**
| 시험 | 결과 |
| --- | --- |
| 가짜 환경 (`tests/stub_test.lua`, LuaLS 실행 파일의 Lua 5.5) | 44줄 설치, 1.1 조건, 인덱스 어림 88 |
| MSF_UE_RE 빌드 `SNQC_LUA` (tepc 2026-09-05 판 `Documents	epc-win	epc.exe` → euddraft → CPLP) | 통과. `C:\Program Files (x86)\StarCraft\Maps\마린키우기_UnLimit_ExceeD_SCR_DB(_out).scx` |
| 같은 맵 `SNQC_PY` / `MSQC` | 둘 다 통과 (작업 폴더에만). MSQC 단락은 예전 eds 와 칭호 결과 칸만 다르다 |
| 인게임 | 아직 |

빌드 환경 메모: `build_scrdb.py` 가 찾는 `theSeed	ools	epc_20260905.exe` 가 이 PC 에 없었다 (theSeed 쪽은 적층판 `tepc.exe` 로 바뀜).
음원 폴더 `C:\euddraft0.9.2.0\MSF_UE_RE_BGM` 도 없어서 `split_map.py --base <작업 폴더>` 로 음원만 뽑았다 (저장소의 기본 맵은 그대로).

## 9. MSF_UE_RE 인게임 1차 (2026-09-17) → 1.2

- 제작자 시험: 키(Y 로 게임 시작)는 되는데 **런처 불러오기가 안 된다**.
- 런처 `Log.txt`: 연결·매니페스트(항목 지문) 정상. "보낼 항목 6개" 에서 **워드 15개 / 10개까지 받고 멈춤** (예전 MSQC 빌드는 0.3~1초에 완료).
  런처는 채널마다 받은 워드 수가 오를 때까지 2초씩 9번 기다리므로, 일부 채널이 거의 안 받는 상태였다.
- 짚어 본 것: 지형(타일셋을 CASC 에서 꺼내 봄 - 맵 전체가 건설 불가 타일인데 대부분 채널은 동작하므로 원인 아님),
  OpenBW 선택·오더 조건(위치와 무관), 맵이 데스 182~189 를 쓰는지(안 씀).
- **원인**: 플러그인 판과 비교하다 찾음. 플러그인 판·MSQC 는 채널 건물의 세대(+0xA5)를 0 으로 두고 ID = 인덱스+1 을 쓰는데,
  Lua 판은 CtrigAsm `f_EPDToAlphaID`(세대 × 2048 + 인덱스 + 1, 세대 전체 바이트)를 썼다. 게임은 세대 mod 32 로 비교하고,
  세대가 32 이상이면 넘친 비트가 패킷의 명령 바이트 0x15 를 바꾼다. 이 맵은 유닛이 계속 생기고 없어져서 재사용 칸의 세대가 높다.
- 1.2: ID 를 직접 계산 (인덱스 + 1 + (세대 mod 32) × 2048). `build.bat` 기본 빌드로 다시 만듦. 인게임 재확인 필요.

## 10. MSF_UE_RE 인게임 2차 (2026-09-17) - 4·8번째 패킷

- 1.2(유닛 ID 수정) 빌드도 불러오기 72.6초. 시험판(`DebugAddr`)과 `MSF_UE_RE/tools/snqc_probe.py` 로 게임 중 채널 상태를 읽고,
  런처와 같은 방식으로 SCR_DB 채널에 시험 워드(꼬리표 3)를 직접 넣어 봤다.
  - 혼자 보내면 모든 채널·모든 값(항목 키 20개, 16비트 전 범위, 하위/상위 꼬리표)이 들어간다.
  - 여럿을 동시에 보내면 **한 턴의 4·8번째 패킷**만 사라진다 (앞선 BGM·Always 줄 2개 포함한 순번).
- 원인: 패킷 시작 위치 7 + 15k 가 4의 배수인 것 → CtrigAsm `f_Memcpy` 의 "대상 4의 배수 / 원본 4로 나눠 3 남음" 경로의
  끝자투리 마스크 오타(`0x00FFFF000`). 오더 바이트 0x28 이 깨져 랠리가 아니게 됐다. 플러그인 판은 eudplib `f_memcpy` 라 없던 문제.
- 고침 1(중간 판): 복사 길이를 4의 배수로 (16 / 28바이트). 결과: 불러오기 0.4초, 8채널 동시 전송 5회·3채널 3회 모두 도착.
- 고침 2(제작자 요청): SNQC 안에 고친 복사 함수 `Call_Copy` 를 두고 f_Memcpy 를 전부 대신함. 마스크를 계산으로 만들고 모의 실행으로 확인. 인게임 재확인 전.
- 같은 날 채널 자리를 각 플레이어 배럭 밑 4×4 로 옮김 (`PlayerXY`, `Columns`) - 시야 0 이어도 자기 칸 주변이 밝혀져 시야 공유로 남에게 보였다.
